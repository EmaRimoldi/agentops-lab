# Figures

These are the current public figures for the swarm baseline study. They are
generated by `scripts/plot_swarm_baselines.py` from the preserved summary data.

| file | message |
| --- | --- |
| `figure-01-experiment-scorecards.png` / `.pdf` | readable names and key facts for each preserved experiment |
| `figure-02-performance-comparison.png` / `.pdf` | swarm runs reached lower validation BPB than the independent-parallel baseline |
| `figure-03-budget-and-throughput.png` / `.pdf` | the four model-comparison swarm runs used the same 120-minute budget, while training-attempt count varied by model |
| `figure-04-swarm-memory-architecture.png` / `.pdf` | swarm agents use isolated workspaces plus a shared JSONL blackboard |
| `figure-05-independent-parallel-architecture.png` / `.pdf` | independent parallel agents run side-by-side without mid-run communication |
